%h2
  Modifier mon profil
= form_for(resource, :as => resource_name, :url => registration_path(resource_name), :html => { :method => :put, :class => "well" }) do |f|
  = devise_error_messages!

  = f.label :username
  = f.text_field :username, { :class => "span3" }

  = f.label :email
  = f.email_field :email, { :class => "span3" }

  = f.label :password
  = f.password_field :password, { :class => "span3" }
  %span.help-inline Laisser vide si vous ne voulez pas le changer

  = f.label :password_confirmation
  = f.password_field :password_confirmation, { :class => "span3" }

  = f.label :current_password
  = f.password_field :current_password, { :class => "span3" }
  %span.help-inline Nécessaire pour confirmer la modification du profil
  %br
  = f.submit "Enregistrer", :class => "btn"

%h3 Supprimer mon compte
%p
  Je ne veux pas rester ici... #{link_to "Supprimer mon profil", registration_path(resource_name), :confirm => "Are you sure?", :method => :delete, :class => "btn btn-danger"}.

= link_to "Retour", :back
